# 키 추출
dict_data.keys()

# 밸류 추출
dict_data.values()

# 키로 밸류 가져오기(name의 키로 밸류 가져오기)
dict_data.get('name') 

# 리스트에 원소를 추가
append함수를 사용하여 하나의 원소씩 입력 가능. 하지만 리스트 형태의 원소를 append하면 [[]] 이런 식으로 넣어진다. 그렇다면 리스트 채로 넣고 싶다면 extend() 함수를 사용. 더불어 + 키(덧셈 연산자)를 사용해도 가능하다.

# 문자열 생성
''' '''

# 인덱싱
첫번째 글자 추출 : ___[0]
마지막 글자 추출 : ___[-1]
